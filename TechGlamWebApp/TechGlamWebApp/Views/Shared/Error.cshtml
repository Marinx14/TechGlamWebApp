@model ErrorViewModel

@{
    ViewData["Title"] = "Error";
}

<h1 class="text-danger">Oops! An error occurred.</h1>
<h2 class="text-danger">We apologize for the inconvenience.</h2>

@if (Model.ShowRequestId)
{
    <p>
        <strong>Error ID:</strong> <code>@Model.RequestId</code>
    </p>
}

<h3>Development Mode</h3>
<p>
    Switching to the <strong>Development</strong> environment will provide more detailed information about the error that occurred.
</p>
<p>
    <strong>However, enabling the Development environment in deployed applications is not recommended.</strong>
    It may expose sensitive information to end users.
    For local debugging, enable the <strong>Development</strong> environment by setting the <strong>ASPNETCORE_ENVIRONMENT</strong> environment variable to <strong>Development</strong>
    and restart the application.
</p>
